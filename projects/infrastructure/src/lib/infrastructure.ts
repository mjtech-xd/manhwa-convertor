// Infrastructure adapters live in subfolders (gemini/, openrouter/, ai33/,
// storage/, key-rotator/, ipc/, logging/). This file exists only as a
// placeholder so ng-packagr has an entry point; real code goes via
// public-api.ts re-exports.

export const INFRASTRUCTURE_LIB_VERSION = '0.1.0';
